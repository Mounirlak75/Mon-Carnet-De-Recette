{% extends 'base.html.twig' %}

{% block title %}Crée une nouvelle catégorie{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h2 class="mb-0">Crée une nouvelle catégorie</h2>
                </div>
                <div class="card-body">
                    {{ include('admin_categorie/_form.html.twig', {'button_label': 'Créer'}) }}

                    <a href="{{ path('app_admin_categorie_index') }}" class="btn btn-secondary mt-3">Retour à la liste des catégories</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
