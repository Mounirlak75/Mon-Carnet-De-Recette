{% extends 'base.html.twig' %}

{% block title %}Etape{% endblock %}

{% block body %}
    <h1>Etape</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etape.id }}</td>
            </tr>
            <tr>
                <th>Text</th>
                <td>{{ etape.text }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ etape.image }}</td>
            </tr>
            <tr>
                <th>Temps</th>
                <td>{{ etape.temps }}</td>
            </tr>
            <tr>
                <th>Numero_etape</th>
                <td>{{ etape.numeroEtape }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_etape_index') }}">back to list</a>

    <a href="{{ path('app_admin_etape_edit', {'id': etape.id}) }}">edit</a>

    {{ include('admin_etape/_delete_form.html.twig') }}
{% endblock %}
